<UserControl x:Class="PlaqueManager.Views.DeadDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cal="http://www.caliburnproject.org"
             ResxExtension.DefaultResxName="PlaqueManager.Views.MainWindow"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid Name="DeadGrid"
                  Style="{x:Null}"
                          CanUserAddRows="True"
                          CanUserDeleteRows="False"
                          SelectionUnit="FullRow"
                          AutoGenerateColumns="False"
                          LoadingRow="DeadGrid_OnLoadingRow"
                          ItemsSource="{Binding Path=List}"
                          cal:Message.Attach="[Event RowEditEnding] = [Action EditRow($executionContext)];
                          [Event SelectionChanged] = [Action OnSelectionChanged($executionContext)]"
                          >
            <DataGrid.Columns>
                <DataGridTextColumn Header="{Resx ResxName=PlaqueManager.Views.MainWindow, Key=DeadTab_Dead}" Binding="{Binding Path=DeadName, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                <DataGridTextColumn Header="{Resx ResxName=PlaqueManager.Views.MainWindow, Key=DeadTab_Live}" Binding="{Binding Path=LiveName, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                <DataGridTextColumn Header="{Resx ResxName=PlaqueManager.Views.MainWindow, Key=DeadTab_Relation}" Binding="{Binding Path=Relation, UpdateSourceTrigger=PropertyChanged}"></DataGridTextColumn>
                <DataGridTemplateColumn Header="{Resx ResxName=PlaqueManager.Views.MainWindow, Key=IsPrint}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="Images/PrintFlag.png" 
                                           Visibility="{Binding Path=IsPrint, Converter={StaticResource visibilityconverter}, Mode=OneWay, TargetNullValue=0, FallbackValue=0}" 
                                           Width="16" Height="16"></Image>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="{Resx ResxName=PlaqueManager.Views.MainWindow, Key=LastPrintHeader}" Binding="{Binding Path=LastPrint, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
