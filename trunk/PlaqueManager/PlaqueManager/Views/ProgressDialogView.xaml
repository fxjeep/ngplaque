<UserControl x:Class="PlaqueManager.Views.ProgressDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="10">
        <TextBlock x:Name="Title" Text="{Binding Path=Title}" TextTrimming="CharacterEllipsis" />
        <TextBlock x:Name="SubTextLabel" Margin="0 18 0 0" Text="{Binding Path=SubText}" FontStyle="Italic" Visibility="Collapsed" TextTrimming="CharacterEllipsis" />
        <ProgressBar x:Name="ProgressBar" Margin="0 38 74 0" IsIndeterminate="True" 
                     Minimum="0" Maximum="100" Value="{Binding Path=CurrentValue}"/>
        <Button x:Name="CancelButton" Margin="0 38 0 0" Width="70" Content="Cancel" HorizontalAlignment="Right" />
    </Grid>
</UserControl>
